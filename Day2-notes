	1. Podman ps:- to see only running containers
	2. Podman ps -a:- to see all containers including stopped n running
	3. Podman restart <container-id or container name>
	4. Podman exec -it <container-id or container name> bash: to login into the container 
	5. Podman stop <container-id or container name>: to stop the containers
	6. Podman pause <container-id or container name>: to pause the container
    7. Podman unpause  <container-id or container name>: to unpause container
    	8. Podaman inspect <container-id or container name>: To inspect any container
	9. Curl ip-add:port-no
	10. Podman run:- It will do two things , one is it will create the container and secondly it will start the container
	11. Podman create:- it will create the container only but will not start the container
	12. Podman rm :- To remove only stooped containers
	13. Podman rm -f : To remove running containers without stopping
	14. Podman rmi <image-id or image-name> : to remove the podman images
    	15. Podman rmi $(podman images -q): to delete all unused podman images
	16. Podman rmi -f $(podman images -q): to delete used images.

==================================================================

Pulling images
============

podman pull python:bullseye

podman pull busybox:unstable-musl

podman pull nginx:stable-perl

